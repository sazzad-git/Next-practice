const page = async () => {
  // console.log(postData);
  return (
    <div>
      <h2>This is user list</h2>
    </div>
  );
};

export default page;

export const generateMetadata = () => {
  return {
    title: "This is a userList",
    description: "this is hablu programmer website",
  };
};
